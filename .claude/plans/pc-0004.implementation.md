# PC-0004: Billing Settings — Handle `physical_contract`

## Summary

Modify `billing-settings.tsx` to detect `paymentProvider: 'physical_contract'` and show "Contrato físico" label, hide renewal date, and hide upgrade/actions section.

## Changes

### Modify: `src/components/billing/billing-settings.tsx`

1. Add `isPhysical` flag alongside existing `isStripe`/`isWompi`:

   ```ts
   const isPhysical = subscription?.paymentProvider === 'physical_contract'
   ```

2. **"Próximo cobro"** — hide when `isPhysical` (no renewal applies):
   - Wrap billing interval/next charge block with `!isPhysical` guard

3. **"Método de pago" card** — add `isPhysical` case:
   - Show "Contrato físico (administrado manualmente)" text

4. **"Acciones" card** — hide entirely when `isPhysical`:
   - Admin controls tier, user shouldn't see upgrade/billing buttons

## Files

- `src/components/billing/billing-settings.tsx` (modify)
