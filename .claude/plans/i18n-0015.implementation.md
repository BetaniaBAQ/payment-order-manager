# i18n-0015: Migrate constants and remove TOAST_MESSAGES

## Summary

Clean up legacy constant files. `TOAST_MESSAGES` already unused (all call sites migrated to `t()`). `BREADCRUMB_LABELS` already unused (route pages use `t()` directly). Delete dead code and remove re-exports.

## Changes

### Delete

- `src/lib/constants/messages.ts` — `TOAST_MESSAGES` + `DEFAULT_ERROR_MESSAGE` no longer imported anywhere

### Edit

- `src/lib/constants/navigation.ts` — Remove `BREADCRUMB_LABELS` export (unused)
- `src/lib/constants/index.ts` — Remove `export * from './messages'`

### Verify

- `pnpm check` passes
- No broken imports (grep for `TOAST_MESSAGES`, `BREADCRUMB_LABELS`, `DEFAULT_ERROR_MESSAGE`, `from '@/lib/constants/messages'`)

## Files

- `src/lib/constants/messages.ts` (delete)
- `src/lib/constants/navigation.ts` (edit)
- `src/lib/constants/index.ts` (edit)
- `specs/tasks/i18n-user-preferences/progress.md` (mark done)
- `specs/handoff.md` (update)
