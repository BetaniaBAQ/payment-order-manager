# i18n-0002: Extend Convex users schema with preferences

## Summary

Add `language` and `theme` optional fields to users table. Extend `update` mutation to accept them.

## Changes

1. **`convex/schema/users.ts`** — add `language: v.optional(v.string())` and `theme: v.optional(v.string())` after `avatarUrl`
2. **`convex/users.ts`** — add `language`/`theme` to `update` mutation args + handler updates object

## Files

- `convex/schema/users.ts` (edit)
- `convex/users.ts` (edit)
