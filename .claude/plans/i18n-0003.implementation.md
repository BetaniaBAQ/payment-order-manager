# i18n-0003: Preferences Zustand store

## Summary

Create preferences Zustand store with `language`/`theme` state, localStorage persistence, nested actions pattern per zustand rules.

## Changes

1. **`src/stores/preferencesStore.ts`** (new) — Zustand store with:
   - State: `language: 'es'`, `theme: 'dark'`
   - Actions: `setLanguage`, `setTheme`, `setPreferences` (batch)
   - Exports: `useLanguage()`, `usePreferredTheme()`, `usePreferencesActions()`
   - Persist key: `preferences-store`, devtools name: `PreferencesStore`

## Files

- `src/stores/preferencesStore.ts` (new)
