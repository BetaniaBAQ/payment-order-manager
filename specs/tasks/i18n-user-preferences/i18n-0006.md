# 0006: Wire i18n into root layout

## Context

Import the i18n module at app entry point and set the default HTML lang attribute to Spanish.

## Dependencies

- 0001 (i18n module exists)

## Files

- `src/routes/__root.tsx` (edit)

## Requirements

1. Add `import '@/i18n'` near top of file (after library imports, before local imports)
2. Change `<html lang="en"` to `<html lang="es"`
3. `suppressHydrationWarning` is already present — no change needed
4. `ThemeProvider` config unchanged — `defaultTheme="dark"` stays as server-side default

## No-gos

- Do not add an `I18nextProvider` wrapper — `initReactI18next` plugin handles React integration automatically
- Do not change `ThemeProvider` settings

## Definition of Done

- [ ] `import '@/i18n'` in `__root.tsx`
- [ ] `<html lang="es">` as default
- [ ] App renders without SSR hydration warnings
- [ ] `useTranslation()` hook works in any component
